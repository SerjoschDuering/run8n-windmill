Pull latest from Windmill remote for the current workspace.

First, identify which workspace folder we're in (check for wmill.yaml).
Then run: `wmill sync pull --yes`

Show a summary of what changed.
